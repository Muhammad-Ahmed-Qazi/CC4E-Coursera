// You should write a function called faren(cel) that takes as input a double Celsius temperature and converts it and returns the equivalent Fahrenheit value.

double faren(double cel) {
    return (cel * 9.0 / 5.0) + 32.0;
}